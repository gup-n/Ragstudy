RAG 文档加载管线使用指南

本文档介绍如何使用本项目的文档加载模块。

支持的文档格式包括 TXT、Markdown、PDF、DOCX 四种。
所有格式统一输出为 page_content + metadata 的结构。

使用方法：
1. 将文档放入 Data/docs/ 目录
2. 运行 uv run python main.py
3. 查看加载结果统计

注意事项：
- 文本文件必须为 UTF-8 编码
- PDF 仅支持可提取文本的类型，扫描件不支持
- 不支持的格式会被自动跳过
