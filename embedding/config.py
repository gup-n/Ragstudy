"""Embedding 模块配置——所有常量集中在此。

其他模块统一 `from embedding.config import ...` 引用。
修改路径、密钥、缓存等参数只需改这里。
"""

from pathlib import Path

# ---------------------------------------------------------------------------
# 项目路径
# ---------------------------------------------------------------------------

# 项目根目录（embedding/config.py 所在目录的父目录的父目录）
PROJECT_ROOT = Path(__file__).resolve().parent.parent

# 数据目录
DATA_DIR = PROJECT_ROOT / "Data"

# ---------------------------------------------------------------------------
# 数据库
# ---------------------------------------------------------------------------

# SQLite 数据库文件路径
DATABASE_PATH = DATA_DIR / "rag.db"

# 是否启用 SQLite WAL 模式（提升并发读性能）
ENABLE_SQLITE_WAL = True

# ---------------------------------------------------------------------------
# API Key 加密
# ---------------------------------------------------------------------------

# 加密密钥文件路径（自动生成，不可跨机器复制）
ENCRYPTION_KEY_PATH = DATA_DIR / "encryption.key"

# ---------------------------------------------------------------------------
# HuggingFace 模型缓存
# ---------------------------------------------------------------------------

# HuggingFace 模型本地缓存根目录
HF_MODEL_CACHE_DIR = DATA_DIR / "hf_cache"

# ---------------------------------------------------------------------------
# Ollama 默认配置
# ---------------------------------------------------------------------------

# Ollama 服务默认地址
OLLAMA_DEFAULT_BASE_URL = "http://localhost:11434"
